{
  "name": "algovision",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client start\" \"npm --prefix server run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
